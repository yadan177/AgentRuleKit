# 公开前自动筛查记录（2026-09-24）

本记录只覆盖仓库快照 `d226c2d` 与当时 `git rev-list --all` 可达的 29 个提交。仓库仍是 Private；以下结果不构成内容授权、法律结论或“可以公开”的批准。

## 已检查范围与结果

| 检查项 | 结果 | 局限 |
|---|---|---|
| 文件清单 | 当前跟踪 395 个文件；`rulepacks/` 132 个文件，`archive/legacy-rules/` 157 个文件；历史中另有 30 个已删除路径 | 文件数量不能说明内容来源或许可 |
| 常见凭据格式 | 当前文件及全部可达提交的文本中，未命中常见 GitHub/OpenAI/AWS 访问令牌、私钥头和长 Bearer 令牌模式 | 非标准凭据、图片中的信息及未被模式覆盖的秘密仍可能存在 |
| 邮箱、主机与本机路径 | 当前文件内容中的邮箱域名仅见 `example.com`；历史内容另有 `sentry.io` 示例 DSN 形式。命中的本机路径是 `C:\Users\Name\Documents` 示例；未检出指向 `.internal`、`.corp` 的 HTTP URL | 提交元数据另含一个 `163.com` 个人邮箱域名，公开仓库时 Git 历史会暴露该元数据；具体邮箱未写入本记录 |
| 二进制附件 | 当前文件及全部可达提交的文件名中，未发现常见图片、PDF、Office、视频、密钥或压缩包扩展名 | 非典型扩展名或嵌入文本内容未据此排除 |
| npm 生产依赖 | `npm audit --omit=dev --json` 报告 0 个已知漏洞 | 仅代表检查时注册表的已知公告，不覆盖开发依赖、恶意包或未来公告 |
| CI 权限 | 普通 CI 默认改为 `contents: read`；只有标签 Release 作业申请 `contents: write`。npm 发布工作流仅手动触发且要求版本标签，申请 `id-token: write` | Actions 与依赖的供应链风险仍需发布前复核 |
| Codex 会话 Hook | 仅在项目锁文件声明 GitHub 来源且用户信任 Hook 后，访问 `api.github.com` 查询最新 Release；缓存写入个人插件数据目录，不修改业务项目 | 用户须审查并信任 Hook；检查会向 GitHub 请求锁文件声明的仓库名 |

## 尚需人工决定

1. 逐份确认 14 个规则包、116 份 Markdown 规则的作者、来源以及公开分发权限。代码示例和外部资料引用也须检查，不能由扫描器推断授权。
2. 单独决定 `archive/legacy-rules/` 的 157 个当前文件及其历史内容是否可以公开；即使停止安装归档内容，它们仍会随公开仓库和 Git 历史可见。
3. 确认 Git 提交者个人邮箱元数据是否可公开。若不能接受，须先讨论可审查的历史处理方案；不得直接改写当前共享历史。
4. 人工核查历史中的 Sentry DSN 示例及所有未被模式覆盖的潜在敏感信息，尤其是客户名称、内部业务代码与第三方版权内容。
5. 仓库所有者确认 Apache-2.0 的适用范围、公开范围与 npm 包所有权后，再执行发布说明中的外部环境验收。

自动筛查没有发现明确的密钥泄露，但**内容权属、历史公开范围和提交者元数据尚未获确认**。因此继续保持 Private，不创建发布标签、GitHub Release 或 npm 包。
