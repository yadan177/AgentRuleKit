# 公开仓库复扫记录（2026-09-25）

本记录对应 `main` 提交 `11662f49ef3752c9bdae8df0c10876f70f6c79f6`，补充[2026-09-24 的历史筛查记录](publication-audit-2026-09-24.md)。仓库与 `v0.1.0`、`v0.1.1` 已公开发布；本次复扫不是发布前批准，也不替代[内容权利复核](content-rights-review.md)与人工隐私审查。

## 检查范围

运行 `node scripts/audit-git-history.mjs --review-locations`，检查本机 `git rev-list --all` 可达的 87 个提交、2051 个对象。其中 1161 个 blob 均为扫描器支持的 UTF-8 文本且已扫描；跳过的二进制与超过 2 MiB 的 blob 均为 0。候选位置模式不会输出匹配值。这个范围不包括本机尚未获取的远端引用，也不保证覆盖已被删除或不可达的 Git 对象。

| 模式结果 | 数量 | 位置与复核结论 |
|---|---:|---|
| 内部 URL 候选 | 1 | `scripts/tests/audit-git-history.test.mjs` 第 17 行的合成测试样例；不是发现的真实内部服务地址。 |
| URL 用户信息候选 | 1 | 历史 blob `16b01a4045af` 中旧路径 `js_ts_rulers/js_rulers/其他规则/07-日志调试规则.md` 第 101 行。只核对了 URL 结构：主机为 `sentry.io`，用户名与路径均为 `xxx` 占位符，没有密码；未发现这处候选是可用凭据的证据。 |
| 提交元数据中的非示例邮箱 | 2 个不同地址 | 其中一个属于 GitHub 合并机器人，另一个是 `163.com` 域名的个人邮箱。出于隐私考虑，本记录不写出个人地址；它已随公开 Git 历史可见，是否继续公开及相关处理仍由邮箱本人/仓库所有者决定。 |

常见 GitHub、OpenAI、AWS、Slack、Stripe 令牌格式、私钥头与长 Bearer 令牌模式没有命中。运行 `npm audit --json`，检查时生产、开发与可选依赖的已知漏洞总数均为 0。漏洞库结果会变化，不能作为未来安全保证。

## 尚未由机器检查解决的事项

- 14 个规则包的 116 份 Markdown、历史归档的 157 份文件及 Git 历史中的旧版本，仍需要内容负责人逐份补录作者、来源与可分发依据；当前[复核表](content-rights-review.md)未把任何文件记为已核。
- 模式扫描不能鉴定非标准秘密、客户名称、个人资料、代码示例的权属，也不能证明截图、未获取的远端历史或第三方材料没有风险。
- Codex 桌面端的四个 Skills 实际调用、Hook 信任选择与新会话调度结果仍需在[桌面端验收记录](codex-desktop-acceptance.md)中填入实际证据。脚本级联网测试不能替代这项验收。

本次没有修改规则正文、历史归档、已发布 Release 或 npm 包。
