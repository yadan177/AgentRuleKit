# JavaScript AI 编码规则 · 文档总览（AI 入口）

---

## 🎯 规则撰写的核心原则

1. AI 编程规则的本质，是将项目中的架构边界、业务约束与编码规范转化为机器可执行的决策规则，使 AI 输出的不只是能运行的代码，而是符合团队工程标准、可维护且可扩展的代码。
2. 不要有 AI 幻觉。
3. 不要过度设计。

---

## 1. 适用范围

- ✅ JavaScript 项目开发（Node.js / 浏览器 / React / Vue / 通用 JS，按项目实际运行时选择）
- ✅ Codex / Qoder / TRAE 及其他可读取项目规则的 AI 工具

> Node.js、浏览器、React、Vue、构建工具、测试工具和样式方案均以仓库已有运行时、依赖和脚本为准；本规则中的示例不构成默认技术栈。

❌ 不适用：TS 独有类型规则（见 `../ts_rulers/`）/ 框架深度用法

---

## 2. 硬性约束

| 等级 | 标识 | 含义 |
|------|------|------|
| 强制 | 🔴 | 100% 遵守，违反即报错 / 崩溃 / 错误 |
| 推荐 | 🟡 | 偏离需说明理由 + 注释 |
| 可选 | 🟢 | 按项目实际灵活选择 |
| 强制 | 🟠 | 安全红线，违反即漏洞（见 11） |

---

## 3. 文档总览

| 编号 | 文档 | 任务查表 | 文档定位 |
|------|------|---------|---------|
| 01 | 代码编写规则 | 写新函数 / 命名 / 改函数 / 写新文件 | 命名 / 引用 / 解构 / 字符串 / 函数 / 类 / 模块 / 控制流 / 注释 |
| 02 | 模式设计规则 | 设计模块接口 / 选模式 / 决定分层 | 模块组织 / OOP vs FP / 错误传播 / Promise / 状态管理 / 设计模式 |
| 03 | 性能优化规则 | 写热路径 / 排查性能 / 加 benchmark | V8 引擎 / DOM / 长列表 / 内存 / 网络 / 打包 / Node 后端 |
| 04 | 异步与事件循环 | 写 async / await / Promise / 排查事件循环问题 | Promise / async-await / 微任务 / 宏任务 / AbortController / 并发限流 |
| 05 | 数据访问规则 | 读写存储 / 发 HTTP / 选存储方案 | localStorage / Cookie / IndexedDB / fetch / Cache API / 跨标签页通信 |
| 06 | 项目配置规则 | 新建项目 / 改构建配置 / 配 ESLint / Prettier | package.json / Vite / Webpack / ESLint / Prettier / 环境变量 / 目录结构 |
| 07 | 日志调试规则 | 加日志 / 排查 Bug / 配 Source Map / 错误上报 | console 5 级 / debugger / Source Map / Sentry / 结构化日志 / 性能日志 |
| 08 | 测试规则 | 写单元测试 / 配 Mock / 看覆盖率 / TDD | Vitest / Jest / Mock / 异步测试 / 覆盖率 / TDD / E2E |
| 09 | 版本控制规则 | 写 commit / 配 .gitignore / 提 PR / 配 CI | Conventional Commits / .gitignore / LFS / 分支 / PR / CI |
| 10 | 代码风格规则 | 改风格 / 配 EditorConfig / 配 Prettier | Airbnb / Standard / Prettier / EditorConfig / 命名 / 注释风格 |
| 11 | 安全规则 | 处理用户输入 / 认证 / 鉴权 / 加密 / 敏感数据 | XSS / CSRF / CSP / 注入 / 认证 / 加密 / 依赖安全（🟠 强制）|
| 12 | JS 常见反模式 | 写完代码自查 / 排查旧代码坑 | var 提升 / == 隐式转换 / forEach 滥用 / 吞错误 / 闭包泄漏等 |
| 13 | React 组件规则 | 写 React 组件 / 配 useEffect / 设计 Props | useEffect 依赖 / 状态放哪 / Props / 组件拆分 / z-index |
| 14 | CSS 与样式体系规则 | 选样式方案 / 写样式 / 配 Tailwind / 配 CSS-in-JS | Tailwind / CSS-in-JS / CSS Modules 三选一 / !important / 颜色变量 |
| 15 | AI 通用入口规则 | AI 开始任务 / 审查 / 排障 / 维护规则 | 项目事实检查 / 变更边界 / 专题路由 / 验证与交付 |

> **TS 项目**：TS 项目必查本目录 14 份 + `../ts_rulers/` 8 份 TS 独有规则，共 22 份

---

## 4. 文档关系图

```
═══════════════════════════════════════════════════════════
                        入口层
═══════════════════════════════════════════════════════════
                              ┌───────────────────────┐
                              │ 00 文档总览（本文件）  │
                              └───────────────────────┘
                                       ↕
                              ┌───────────────────────┐
                              │ 15 AI 通用入口规则     │
                              └───────────────────────┘
                                       ↕
                              ┌───────────────────────┐
                              │ 12 常见反模式（自查）   │
                              └───────────────────────┘


═══════════════════════════════════════════════════════════
        基础编码层（写任何 JS 代码都要遵守的底层约束）
═══════════════════════════════════════════════════════════
┌───────────────────────┐  ┌───────────────────────┐
│ 01 代码编写规则        │  │ 10 代码风格规则        │
│ 命名 / 引用 / 函数 /  │  │ Prettier / EditorConfig│
│ 类 / 模块 / 控制流     │  │ / Airbnb / Standard     │
└───────────────────────┘  └───────────────────────┘
           ↖────────── ↕ 同步进行 ↕ ──────────↗
                        ┌───────────────────────┐
                        │ 02 模式设计            │
                        │ ★ 业务逻辑设计入口     │
                        └───────────────────────┘


═══════════════════════════════════════════════════════════
        运行时质量层（性能 / 异步 / 数据）
═══════════════════════════════════════════════════════════
┌───────────────────────┐  ┌───────────────────────┐  ┌───────────────────────┐
│ 03 性能优化            │  │ 04 异步与事件循环      │  │ 05 数据访问           │
│ V8 / DOM / 内存 /    │  │ Promise / async /     │  │ localStorage / fetch  │
│ 打包 / Node           │  │ AbortController / 并发 │  │ / Cookie / IndexedDB  │
└───────────────────────┘  └───────────────────────┘  └───────────────────────┘


═══════════════════════════════════════════════════════════
        工程协作层（日常开发高频接触）
═══════════════════════════════════════════════════════════
┌───────────────────────┐ ┌───────────────────────┐ ┌───────────────────────┐
│ 06 项目配置           │ │ 07 日志调试           │ │ 09 版本控制           │
│ Vite / Webpack /     │ │ Sentry / Source Map / │ │ Conventional Commits / │
│ ESLint / Prettier     │ │ console 5 级 / 脱敏    │ │ LFS / PR / CI         │
└───────────────────────┘ └───────────────────────┘ └───────────────────────┘


═══════════════════════════════════════════════════════════
        质量保障层（部署前必查）
═══════════════════════════════════════════════════════════
┌───────────────────────┐ ┌───────────────────────┐
│ 08 测试               │ │ 11 安全 🟠             │
│ Vitest / Mock / TDD  │ │ XSS / CSRF / CSP /     │
│ / 覆盖率 / E2E        │ │ 注入 / 加密 / 认证     │
└───────────────────────┘ └───────────────────────┘


═══════════════════════════════════════════════════════════
        专项场景层（按 UI 框架按需查阅）
═══════════════════════════════════════════════════════════
┌───────────────────────┐ ┌───────────────────────┐
│ 13 React 组件          │ │ 14 CSS 与样式体系     │
│ useEffect / 状态 /     │ │ Tailwind / CSS-in-JS /│
│ Props / 拆分 / z-index │ │ CSS Modules / 颜色变量│
└───────────────────────┘ └───────────────────────┘
```

---

## 5. 跨语言继承

JS 是基线层（TS 的上游）。TS 项目必须同时加载本目录 14 份 + `../ts_rulers/` 8 份 TS 独有规则。

---

## 6. 文档维护

### 6.1 版本信息

| 项目 | 内容 |
|------|------|
| 当前版本 | v1.1 |
| 发布日期 | 2026-07-16 |
| 维护责任人 | 柚子 |

### 6.2 维护记录

| 版本 | 发布日期 | 发现问题 | 解决方式 |
|------|---------|---------|---------|
| v1.0 | 2026-07-02 | — | 首版发布 |
| v1.1 | 2026-07-16 | AI 工具入口与运行时/框架被表述为默认前提 | 增加通用入口、三工具适配器，并改为项目事实优先 |
