# 01 - TypeScript 类型系统

> 🟢 本文件适用于 TypeScript 类型层硬约束（type vs interface/any/unknown/工具类型）。<br>
> 🔴 不适用：业务逻辑实现、UI 样式设计

---

## 1. 🔴 硬约束 · 这些绝对不要写

### 1.1 type vs interface 选型

```ts
// ✅ interface：对象 / 类的形状（可继承 / 可声明合并）
interface User {
  id: number;
  name: string;
}
interface AdminUser extends User {
  role: 'admin';
}

// ✅ type：联合 / 交叉 / 工具类型 / 计算属性
type Status = 'pending' | 'approved' | 'rejected';
type UserOrError = User | { error: string };
type ReadonlyUser = Readonly<User>;
type Getters<T> = { [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K] };
```

**经验法则**：

| 场景 | 选谁 |
|------|------|
| 对象 / 类的形状（可继承 / 合并） | `interface` |
| 联合 / 交叉类型 | `type` |
| 工具类型（`Partial<T>` 之类） | `type` |
| 需要声明合并（多次 `interface Foo`） | `interface` |
| 元组 / 数组具体形状 | `type` |
| 计算属性 / 映射类型 | `type` |

### 1.2 any 滥用（关闭类型检查）

```ts
// ❌
function parse(input: any) { return JSON.parse(input); }
const data: any = fetchUser();

// ✅ unknown 替代 + 收窄
function parse(input: unknown): User {
  if (typeof input !== 'string') throw new Error('Expected string');
  return JSON.parse(input) as User;        // 已收窄为 string
}
```

**配置 ESLint 强制**：

```js
// eslint.config.js
{
  rules: {
    '@typescript-eslint/no-explicit-any': 'error',
    '@typescript-eslint/no-unsafe-*': 'error',
  },
}
```

### 1.3 unknown 没有收窄就用

```ts
// ❌
function process(input: unknown) {
  return input.name;                       // ❌ 编译报错（unknown 无属性）
}

// ✅ 收窄
function process(input: unknown) {
  if (typeof input === 'object' && input !== null && 'name' in input) {
    return (input as { name: string }).name;
  }
  throw new Error('Invalid input');
}

// ✅ 推荐：zod 校验
import { z } from 'zod';
const UserSchema = z.object({ name: z.string() });
const user = UserSchema.parse(input);      // 类型自动推断为 { name: string }
```

详见 [02-类型守卫与收窄](./02-类型守卫与收窄.md)。

### 1.4 enum 滥用

```ts
// ❌ 数字 enum（不安全，可反向映射）
enum Color { Red, Green, Blue }
const c: Color = 99;                       // 编译通过！没意义

// ❌ const enum（与 isolatedModules 冲突，不推荐）
const enum Direction { Up, Down }

// ✅ 用 as const 对象字面量
const Color = {
  Red: 'red',
  Green: 'green',
  Blue: 'blue',
} as const;
type Color = typeof Color[keyof typeof Color];     // 'red' | 'green' | 'blue'
```

**enum 也不是完全禁用**——字符串 enum 在某些场景仍可用：

```ts
// ✅ 字符串 enum（明确枚举名 + 字符串值）
enum HttpStatus {
  OK = 'OK',
  NotFound = 'NOT_FOUND',
  ServerError = 'SERVER_ERROR',
}
```

**默认推荐 `as const 对象` 模式**（更安全、tree-shaking 友好）。

### 1.5 联合类型误用（不会收窄）

```ts
// ❌ 没法区分的 union
type Shape = { width: number } | { height: number };
function area(s: Shape) {
  return s.width * s.height;               // ❌ 两个都没有两个属性
}

// ✅ 判别联合（discriminated union）
type Shape =
  | { kind: 'rect'; width: number; height: number }
  | { kind: 'circle'; radius: number };
function area(s: Shape): number {
  switch (s.kind) {
    case 'rect': return s.width * s.height;
    case 'circle': return Math.PI * s.radius ** 2;
  }
}
```

### 1.6 泛型滥用（应为具体类型）

```ts
// ❌ 写两个泛型只用一个
function identity<T, U>(x: T, _y: U): T { return x; }

// ✅ 一个泛型解决
function identity<T>(x: T): T { return x; }

// ✅ 用泛型约束
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}
```

### 1.7 工具类型选择不当

```ts
// ❌ 自己造轮子
type MyPartial<T> = { [K in keyof T]?: T[K] };   // 与 Partial 重复

// ✅ 用内置
type UserOptional = Partial<User>;
type UserNameOnly = Pick<User, 'name'>;
type UserWithoutPassword = Omit<User, 'password'>;
type UserReadonly = Readonly<User>;
```

**常用工具类型**：

| 类型 | 作用 |
|------|------|
| `Partial<T>` | 全字段可选 |
| `Required<T>` | 全字段必填 |
| `Readonly<T>` | 全字段只读 |
| `Pick<T, K>` | 选字段 |
| `Omit<T, K>` | 去字段 |
| `Record<K, V>` | key-value 映射 |
| `Exclude<T, U>` | 从 T 排除 U |
| `Extract<T, U>` | 从 T 提取 U |
| `ReturnType<T>` | 函数返回类型 |
| `Parameters<T>` | 函数参数元组 |
| `Awaited<T>` | Promise 解包 |

---

## 2. 🟡 推荐 · 团队约定

### 2.1 命名约定（type / interface）

```ts
// ✅ interface 不加 I 前缀
interface User {}                       // 推荐
interface IUser {}                      // 不推荐（C# 风格）

// ✅ type 别名 PascalCase
type UserId = string;
type Status = 'pending' | 'done';

// ✅ 泛型用单字母
function identity<T>(x: T): T { return x; }
function merge<A, B>(a: A, b: B): A & B { return { ...a, ...b }; }

// 复杂场景可用描述性名字
function fetchEntity<Entity extends { id: string }>(id: string): Promise<Entity> { /* ... */ }
```

### 2.2 导出风格

**具名导出优先**（与 [01-代码编写规则 §7](../../js_rulers/其他规则/01-代码编写规则.md) 一致）：

```ts
// ✅
export interface User {}
export function getUser(id: number) {}

// ❌ 默认导出整个对象
export default { User, getUser };
```

### 2.3 字面量类型与 `as const`

```ts
// ❌ 反复声明字面量类型
const direction: 'up' | 'down' = 'up';

// ✅ 用 as const 一次搞定
const direction = 'up' as const;

// ✅ 字面量对象
const CONFIG = {
  apiUrl: 'https://api.example.com',
  timeout: 5000,
} as const;
type Config = typeof CONFIG;            // { readonly apiUrl: 'https://...'; readonly timeout: 5000 }
```

### 2.4 公共 API 显式返回类型

```ts
// ✅ 公共函数显式返回类型（库代码）
export function getUser(id: number): Promise<User | null> { /* ... */ }

// ✅ 业务代码可依赖推断（更简洁）
const sum = (a: number, b: number) => a + b;     // TS 推断为 (a: number, b: number) => number
```

---

## 3. 🟢 可选 · 视情况

### 3.1 satisfies 运算符（TS 4.9+）

```ts
// ✅ 校验形状 + 保留字面量推断
const colors = {
  red: '#f00',
  green: '#0f0',
} satisfies Record<string, string>;

colors.red;                              // 类型是 '#f00'（不是 string）
```

**vs `as`**：
- `satisfies`：校验形状 + 保留窄类型
- `as`：强制断言类型（**最后手段**）

### 3.2 const 类型参数（TS 5.0+）

```ts
// ❌ 推断成 string[]
function pushAll<T>(arr: T[], items: T[]) { arr.push(...items); }
pushAll(['a', 'b'], ['c', 'd']);        // T = string，items: string[]

// ✅ 推断成 readonly ['c', 'd']
function pushAll<const T>(arr: T[], items: readonly T[]) { arr.push(...items); }
```

### 3.3 模板字面量类型

```ts
type EventName = `on${Capitalize<string>}`;       // 'onClick' | 'onHover' | ...
type ApiPath = `/api/${string}`;                  // 路径约束
```

---
