# 09 - Go 依赖与构建规则

---

> 🟢 本文件适用于 **Go 项目的依赖管理**和**构建配置**——添加/升级依赖、编写 Dockerfile、配置交叉编译、设置编译标志<br>
> 🔴 **不适用**：业务代码（见 01/02）、API 设计（见 04）、数据库配置（见 05）、go.mod 基础字段定义（见 06-§ 1）<br>
> 🟡 本文件提供的模板可以直接复制使用——按需调整模块路径、Go 版本等变量

## 1. go.mod 管理

> 基础结构（`module` / `go` / `require` / `replace` / `exclude`）已在  中覆盖。本节聚焦**日常维护操作**和**依赖管理策略**。

### 1.1 `go mod tidy` 的时机

🔴 必加约束

**任何**涉及 import 变更的操作后，**必须**立即运行 `go mod tidy`：

| 操作 | 是否需要 `go mod tidy` |
|---|---|
| 添加新的 `import` | ✅ 必须 |
| 删除不再使用的 `import` | ✅ 必须 |
| 修改 `go.mod` 中的 `go` 版本 | ✅ 必须 |
| 添加/删除 `replace` / `exclude` | ✅ 必须 |
| 仅修改 `.go` 文件（不涉及 import 变更） | ❌ 不需要 |
| 仅修改注释 | ❌ 不需要 |

```bash
# 标准流程：改完 import → tidy → 验证 → 提交
go mod tidy
go mod verify
git diff --exit-code go.mod go.sum  # 确认无意外变更
```

### 1.2 依赖版本管理

🔴 必加约束

| 指令 | 用途 | 示例 |
|---|---|---|
| `go get pkg@v1.2.3` | 锁定精确版本 | `go get github.com/gin-gonic/gin@v1.9.1` |
| `go get pkg@latest` | 升级到最新版本 | `go get github.com/gin-gonic/gin@latest` |
| `go get -u ./...` | 升级所有直接依赖的次版本 | 谨慎使用——一次性改动太大 |
| `go get -u=patch ./...` | 仅升级补丁版本 | 安全升级，推荐 |

```bash
# 反例：一次性升级所有依赖（容易引入不兼容变更）
go get -u ./...

# 正例：逐个升级，每次只改一个依赖 + 跑测试
go get github.com/gin-gonic/gin@latest
go test ./...
git add go.mod go.sum && git commit -m "chore(deps): upgrade gin to v1.10.0"
```

### 1.3 `replace` 指令的纪律

🔴 必加约束

`replace` 是 Go modules 中最容易被滥用的指令。以下规则必须遵守：

| 场景 | 做法 | 是否提交 |
|---|---|---|
| 本地开发中临时引用未发布的本地模块 | 使用 `go.work`（Go 1.18+），**不修改** `go.mod` | ❌ `go.work` 一般不提交 |
| fork 了上游仓库需要长期使用 | fork 后改 `module` 路径，发布独立版本 | ✅ 作为新模块提交 |
| 上游有 bug，临时用 fork 修复 | 加 `replace` + 注释说明原因和计划移除时间 | ✅ 可临时提交（需注明到期日） |
| 调试时指向本地路径 | 用 `go.work`，**绝不**提交 `replace ../local` | ❌ 不提交 |

```go
// go.mod

// 反例：本地路径 replace 提交到仓库
replace github.com/example/lib => ../lib  // 其他人拉下来编译失败

// 反例：replace 掩盖上游问题
replace github.com/broken/lib => github.com/my-fork/lib v1.2.3-fixed
// 问题：版本号没变，但内容变了——Go module proxy 缓存可能混乱

// 正例：fork 后改 module 路径发布独立版本
// my-fork 的 go.mod: module github.com/my-org/lib-fork
// 原项目的 go.mod: require github.com/my-org/lib-fork v1.2.3

// 正例：临时修复（加注释 + 计划移除日期）
replace github.com/broken/lib => github.com/my-fork/lib v1.2.3-fixed
// TODO(author): 移除日期 2026-08-01。上游 PR #1234 合并后删除此 replace
```

### 1.4 `exclude` 与 `retract` 的使用

🟡 推荐

| 指令 | 用途 | 示例场景 |
|---|---|---|
| `exclude` | 排除上游有已知漏洞/严重 bug 的特定版本 | `exclude github.com/vuln/lib v1.2.0` |
| `retract` | 撤回**自己**发布的有问题的版本 | `retract v1.5.0`（在 go.mod 中声明） |

```go
// go.mod

// 正例：排除有已知漏洞的版本（加注释说明原因）
exclude (
    // CVE-2026-1234: RCE vulnerability in v1.2.0
    github.com/vuln/lib v1.2.0
    // 破坏了向后兼容性
    github.com/broken/lib v2.0.0
)

// 正例：撤回自己发布的版本
// 本模块的 v1.5.0 有严重 bug，撤回它
retract v1.5.0
```

### 1.5 `go.work` 工作区

🟡 推荐（Go 1.18+）

多模块同时开发时，用 `go.work` 替代 `replace`：

```bash
# 初始化工作区
go work init ./server ./lib ./shared

# go.work 内容
go 1.22

use (
    ./server
    ./lib
    ./shared
)
```

```go
// 反例：在 go.mod 中用 replace 指向本地模块
replace github.com/my-org/lib => ../lib
replace github.com/my-org/shared => ../shared

// 正例：用 go.work 统一管理本地模块
// go.work 不提交，go.mod 保持干净
```

> `go.work` 一般**不提交**到版本控制（`.gitignore` 添加 `go.work`），除非团队约定使用 workspace 作为主要开发模式。

---

## 2. 依赖升级策略

### 2.1 安全漏洞扫描

🔴 必加约束：**每次提交前扫描已知漏洞**

```bash
# 扫描直接依赖和间接依赖的已知漏洞
govulncheck ./...

# CI 中集成
govulncheck -test ./...
```

> `govulncheck` 是 Go 官方工具（`golang.org/x/vuln/cmd/govulncheck`），数据源来自 Go 漏洞数据库。它**只报告实际影响代码的漏洞**（通过调用图分析），不报告"仅存在于 go.sum 但未使用的模块"。

### 2.2 Dependabot 配置

🟡 推荐

```yaml
# .github/dependabot.yml
version: 2
updates:
  # Go 模块
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Shanghai"
    # 分组 PR：相关依赖合并为一个 PR
    groups:
      golang-x:
        patterns:
          - "golang.org/x/*"
      uber:
        patterns:
          - "go.uber.org/*"
    # 最多同时打开的 PR 数
    open-pull-requests-limit: 5
    # 自动 rebase
    rebase-strategy: "auto"
    labels:
      - "dependencies"
      - "go"

  # Docker 镜像
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    labels:
      - "dependencies"
      - "docker"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    labels:
      - "dependencies"
      - "ci"
```

### 2.3 依赖升级决策表

🟡 推荐

| 升级类型 | 频率 | 自动化 | 需要人工审查 |
|---|---|---|---|
| 安全补丁（CVE） | 发现后**立即** | ✅ Dependabot 自动提 PR | ❌ 可自动合并（CI 通过） |
| 补丁版本（v1.2.3 → v1.2.4） | 每周 | ✅ Dependabot 自动提 PR | 🟡 快速扫一眼 changelog |
| 次版本（v1.2.3 → v1.3.0） | 每月 | 🟡 Dependabot 提 PR | ✅ 必须审查 changelog + 跑集成测试 |
| 主版本（v1.x → v2.x） | 按需 | ❌ 手动 | ✅ 必须仔细评估迁移成本 |
| `golang.org/x/*` | 每周 | ✅ 分组为一个 PR | 🟡 关注 breaking changes |

### 2.4 依赖审计

🟡 推荐

```bash
# 查看所有依赖（含间接依赖）
go mod graph

# 查看为什么引入了某个依赖
go mod why -m github.com/some/package

# 查看可用的升级
go list -m -u all

# 查看依赖树（需要安装 gotree）
go mod graph | grep 'github.com/some/package'
```

```bash
# 反例：不知道为何引入就盲目删除
go mod tidy  # 但没检查 go mod why

# 正例：先理解依赖链路再决策
go mod why -m github.com/indirect/package
# 输出：
# your-module
# your-module/internal/handler
# github.com/direct/dep
# github.com/indirect/package
# → 这是通过 github.com/direct/dep 间接引入的
```

---

## 3. 语义化版本

### 3.1 模块路径与主版本

🔴 必加约束

Go modules 的版本号与模块路径**强绑定**：

| 主版本 | 模块路径 | `go.mod` 中 `module` |
|---|---|---|
| v0 / v1 | `github.com/user/repo` | `module github.com/user/repo` |
| v2+ | `github.com/user/repo/v2` | `module github.com/user/repo/v2` |

```go
// 反例：v2 版本模块路径不带 /v2
module github.com/user/repo  // ← 错误：v2.0.0 tag 但路径没有 /v2
// 后果：go get 无法正确解析版本

// 正例
module github.com/user/repo/v2  // ← v2 必须带 /v2 后缀
```

### 3.2 版本号规则

🔴 必加约束

遵循 [Semantic Versioning 2.0.0](https://semver.org/lang/zh-CN/)：

```
vMAJOR.MINOR.PATCH
  1    .2    .3

v1.2.3 → v1.2.4  补丁版本：bug 修复，向后兼容
v1.2.3 → v1.3.0  次版本：新功能，向后兼容
v1.2.3 → v2.0.0  主版本：不兼容的 API 变更
```

```bash
# 正例：tag 命名
git tag v1.0.0
git tag v1.0.1   # bug 修复
git tag v1.1.0   # 新功能
git tag v2.0.0   # 破坏性变更

# 反例：tag 命名不规范
git tag 1.0.0       # 缺少 v 前缀
git tag v1.0        # 缺少 patch 版本号
git tag release-1.0 # 非标准格式，go get 无法识别
```

### 3.3 预发布版本

🟡 推荐

```
v1.0.0-alpha.1  # Alpha 预发布
v1.0.0-beta.1   # Beta 预发布
v1.0.0-rc.1     # Release Candidate
v1.0.0          # 正式发布
```

Go modules 中预发布版本**不会**被 `go get pkg@latest` 自动选中——需要显式指定。

### 3.4 v0 版本的特殊性

🟡 推荐

- v0.x.x 表示**API 不稳定**，任何次版本都可能包含破坏性变更
- v0.1.0 → v0.2.0 可以有不兼容改动，Go modules 不会阻止
- 一旦发布 v1.0.0，就承诺了向后兼容

```bash
# v0 阶段：可以随意改 API
git tag v0.1.0
# ... 大改 API ...
git tag v0.2.0  # 允许破坏性变更

# v1 阶段：不能破坏公开 API
git tag v1.0.0
# 如果要破坏 API，必须升主版本
git tag v2.0.0
```

---

## 4. Docker 多阶段构建

### 4.1 最小化 Dockerfile 模板

🔴 必加约束：每个 Go 服务**必须**使用多阶段构建

```dockerfile
# Dockerfile
# ──── 构建阶段 ────
FROM golang:1.22-alpine AS builder

# 安装 git（某些依赖需要）+ ca-certificates
RUN apk add --no-cache git ca-certificates tzdata

WORKDIR /app

# 先复制依赖文件，利用 Docker 层缓存
COPY go.mod go.sum ./
RUN go mod download

# 复制源码并编译
COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
    go build -trimpath -ldflags="-s -w" \
    -o /app/server ./cmd/server

# ──── 运行阶段 ────
FROM alpine:3.20

# 从 builder 复制必要的文件
COPY --from=builder /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/
COPY --from=builder /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
COPY --from=builder /app/server /usr/local/bin/server

# 创建非 root 用户
RUN addgroup -S appgroup && adduser -S appuser -G appgroup
USER appuser

EXPOSE 8080
ENTRYPOINT ["server"]
```

### 4.2 使用 distroless 运行时（进阶）

🟡 推荐：追求最小攻击面时使用

```dockerfile
# ──── 构建阶段（同上）────
FROM golang:1.22-alpine AS builder
# ... 同上 ...

# ──── distroless 运行阶段 ────
FROM gcr.io/distroless/static-debian12:nonroot

COPY --from=builder /app/server /server

EXPOSE 8080
ENTRYPOINT ["/server"]
```

> distroless 镜像不含 shell / 包管理器 / 任何可执行文件——攻击面极小，但无法 `docker exec` 进容器调试。生产环境推荐，开发环境用 alpine。

### 4.3 Dockerfile 优化要点

🔴 必加约束

| 要点 | 反例 | 正例 |
|---|---|---|
| 层顺序：先复制依赖再复制源码 | `COPY . .` 在 `go mod download` 之前 | 先 `COPY go.mod go.sum`，`RUN go mod download`，再 `COPY . .` |
| 非 root 用户 | `USER root`（默认） | `USER appuser`（显式创建） |
| 固定基础镜像版本 | `FROM alpine`（latest） | `FROM alpine:3.20` |
| `.dockerignore` | 无 `.dockerignore` | 排除 `bin/` / `.git/` / `*.test` / `vendor/` |
| 编译静态链接 | `go build` | `CGO_ENABLED=0 go build` |
| 不要用 `:latest` | `FROM golang:latest` | `FROM golang:1.22-alpine` |

```dockerfile
# .dockerignore（必须存在）
.git/
.gitignore
*.md
*.test
coverage.out
bin/
vendor/
.env*
docker-compose*.yml
.idea/
.vscode/
```

### 4.4 多架构构建

🟡 推荐

```bash
# 创建多架构 builder
docker buildx create --name multiarch --use

# 构建并推送多架构镜像
docker buildx build \
    --platform linux/amd64,linux/arm64 \
    --tag my-registry/my-service:v1.0.0 \
    --push .

# Makefile 中集成
.PHONY: docker-buildx
docker-buildx:
	docker buildx build \
		--platform linux/amd64,linux/arm64 \
		--tag $(REGISTRY)/$(APP_NAME):$(VERSION) \
		--push .
```

---

## 5. 交叉编译

### 5.1 基本交叉编译

🔴 必加约束：**发布版本必须禁用 CGO**

```bash
# 标准交叉编译命令
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o server-linux-amd64 ./cmd/server
CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o server-linux-arm64 ./cmd/server
CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -o server-darwin-amd64 ./cmd/server
CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -o server-darwin-arm64 ./cmd/server
CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o server-windows-amd64.exe ./cmd/server
```

### 5.2 Makefile 中集成交叉编译

🟡 推荐

```makefile
# Makefile
GOOS        ?= $(shell go env GOOS)
GOARCH      ?= $(shell go env GOARCH)
BUILD_DIR   ?= ./bin

# 单平台构建（默认）
.PHONY: build
build:
	CGO_ENABLED=0 $(GO) build \
		-trimpath \
		-ldflags="$(LDFLAGS)" \
		-o $(BUILD_DIR)/$(APP_NAME) \
		$(CMD_DIR)

# 全平台构建
.PHONY: build-all
build-all:
	@echo "Building for all platforms..."
	@mkdir -p $(BUILD_DIR)
	GOOS=linux   GOARCH=amd64 $(GO) build -trimpath -ldflags="$(LDFLAGS)" -o $(BUILD_DIR)/$(APP_NAME)-linux-amd64   $(CMD_DIR)
	GOOS=linux   GOARCH=arm64 $(GO) build -trimpath -ldflags="$(LDFLAGS)" -o $(BUILD_DIR)/$(APP_NAME)-linux-arm64   $(CMD_DIR)
	GOOS=darwin  GOARCH=amd64 $(GO) build -trimpath -ldflags="$(LDFLAGS)" -o $(BUILD_DIR)/$(APP_NAME)-darwin-amd64  $(CMD_DIR)
	GOOS=darwin  GOARCH=arm64 $(GO) build -trimpath -ldflags="$(LDFLAGS)" -o $(BUILD_DIR)/$(APP_NAME)-darwin-arm64  $(CMD_DIR)
	GOOS=windows GOARCH=amd64 $(GO) build -trimpath -ldflags="$(LDFLAGS)" -o $(BUILD_DIR)/$(APP_NAME)-windows-amd64.exe $(CMD_DIR)
	@echo "Done. Binaries in $(BUILD_DIR)/"
```

### 5.3 CGO 决策表

🔴 必加约束

| 场景 | `CGO_ENABLED` | 说明 |
|---|---|---|
| 纯 Go 服务（99% 场景） | `0` | 静态链接、跨平台编译零成本、镜像极小 |
| 依赖 C 库（如 SQLite C 驱动） | `1` | 无法交叉编译、需目标平台的 C 工具链、镜像体积大 |
| 需要 DNS 解析的 Linux 容器 | `0` | Go 1.9+ 内置纯 Go DNS resolver，不需要 cgo |
| macOS 本地开发 | 默认 | macOS 上某些系统调用可能需要 cgo |

```go
// 检查项目是否依赖 CGO
// 方法 1：查看依赖中是否有 C 文件
find . -name "*.c" -o -name "*.h"

// 方法 2：尝试禁用 CGO 编译
CGO_ENABLED=0 go build ./...
// 如果编译失败，说明有依赖需要 CGO

// 方法 3：查看 import 的包
// 常见需要 CGO 的包：
// - github.com/mattn/go-sqlite3
// - github.com/confluentinc/confluent-kafka-go
```

---

## 6. 编译标志

### 6.1 推荐编译标志

🔴 必加约束：**生产构建必须使用以下标志**

```bash
go build \
    -trimpath \                     # 从二进制中移除文件系统路径
    -ldflags="-s -w" \             # -s: 去除符号表  -w: 去除 DWARF 调试信息
    -o bin/server \
    ./cmd/server
```

| 标志 | 作用 | 效果 |
|---|---|---|
| `-trimpath` | 移除编译时的绝对路径 | 二进制中不包含开发者机器路径（安全 + 可复现构建） |
| `-s` | 去除符号表 | 减小二进制体积（约 30%） |
| `-w` | 去除 DWARF 调试信息 | 减小二进制体积（约 15%），无法用 `dlv` 调试 |
| `-race` | 启用竞态检测器 | **仅开发/测试**，生产不要用（性能开销大） |

```bash
# 反例：生产构建不带任何标志
go build -o server ./cmd/server
# 问题：二进制包含开发者路径、符号表、调试信息——体积大 + 不安全

# 正例：生产构建
CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o server ./cmd/server
```

### 6.2 注入版本信息

🟡 推荐

```go
// cmd/server/main.go
package main

var (
    Version   = "dev"       // 编译时通过 -ldflags 注入
    Commit    = "unknown"
    BuildTime = "unknown"
)

func main() {
    fmt.Printf("server version %s (commit %s, built %s)\n", Version, Commit, BuildTime)
    // ...
}
```

```makefile
# Makefile 中自动获取 git 信息
VERSION    ?= $(shell git describe --tags --always --dirty 2>/dev/null || echo "dev")
COMMIT     ?= $(shell git rev-parse --short HEAD 2>/dev/null || echo "unknown")
BUILD_TIME ?= $(shell date -u +"%Y-%m-%dT%H:%M:%SZ")

LDFLAGS += -X main.Version=$(VERSION)
LDFLAGS += -X main.Commit=$(COMMIT)
LDFLAGS += -X main.BuildTime=$(BUILD_TIME)
```

### 6.3 可复现构建

🟡 推荐

为保证相同源码始终产生完全相同的二进制（安全审计、供应链安全）：

```bash
# 1. 固定时间戳（避免每次构建时间不同）
# 2. 使用 -trimpath 移除路径差异
# 3. 固定 Go 版本

CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
    go build \
    -trimpath \
    -ldflags="-s -w -buildid= -X main.BuildTime=reproducible" \
    -o server \
    ./cmd/server
```

> Go 1.21+ 支持 `GOFLAGS=-trimpath` 环境变量全局启用。

---

## 7. 与版本控制规则的衔接

> 本节与 [08-版本控制规则](08-版本控制规则.md) 互引。08 负责 Git 层面的规则，本节聚焦**构建如何响应版本事件**。

### 7.1 tag 触发构建

🔴 必加约束

```yaml
# .github/workflows/ci.yml 中增加 release 构建 Job
name: CI

on:
  pull_request:
  push:
    branches: [main]
    tags: ['v*']  # tag 推送时触发

jobs:
  # ... lint / test / tidy（同 06-§ 5）

  release:
    name: Release Build
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [lint, test, tidy]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: 构建全平台二进制
        run: make build-all

      - name: 构建 Docker 镜像
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          docker build -t my-service:${VERSION} .
          docker tag my-service:${VERSION} my-service:latest

      - name: 推送 Docker 镜像
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          VERSION=${GITHUB_REF#refs/tags/}
          docker push my-service:${VERSION}
          docker push my-service:latest

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: bin/
```

### 7.2 版本号与构建的对应关系

🟡 推荐

```bash
# 开发期：版本号 = dev
go build -ldflags="-X main.Version=dev" ./cmd/server

# PR 构建：版本号 = commit SHA
go build -ldflags="-X main.Version=pr-$(git rev-parse --short HEAD)" ./cmd/server

# Tag 构建：版本号 = tag
go build -ldflags="-X main.Version=$(git describe --tags)" ./cmd/server

# 正式发布：版本号 = tag（精确）
git tag v1.2.3  # 触发 CI 构建 → Docker image: v1.2.3
```

### 7.3 构建产物与 .gitignore

🔴 必加约束

```gitignore
# .gitignore
# 构建产物
/bin/
/server
*.exe
*.dll
*.so
*.dylib

# 测试产物
*.test
*.out
coverage.out

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 临时文件
*.log
*.tmp
```

> `bin/` 目录是约定的构建产物输出目录（见 ），必须加入 `.gitignore`。
