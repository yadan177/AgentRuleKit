# 06 - Go 项目配置

> 🟢 本文件适用于 Go 项目的初始化与工程配置（go.mod / .golangci.yml / Makefile / 目录结构 / CI 集成）。

> 🔴 不适用：业务逻辑实现、API 设计、数据库 schema 设计

---

## 1. 项目初始化

🔴 必加约束：新建 Go 项目**必须**使用 Go modules（go.mod），**禁止**使用 GOPATH 模式。

### 1.1 go.mod 必填字段

```go
module github.com/your-org/your-project

go 1.21

require (
    // 第三方依赖
)

require (
    // 间接依赖（自动生成）
)
```

🟡 推荐：

- `module` 用完整的 Git 路径（不用 `myproject` 这种短名）
- Go 版本**至少**为当前 LTS（Go 1.21 / 1.22）
- 用 `go mod tidy` 保持依赖整洁
- 用 `go mod tidy && git diff --exit-code go.mod go.sum` 在 CI 中校验

❌ 不要：

- 用相对路径或单段 module 名（如 `myapp`）
- 手动编辑 `require ( )` 块内的间接依赖
- 混用 GOPATH 项目（`go.mod` 是唯一标准）

### 1.2 项目目录结构

🔴 必加约束：标准库项目（如 SDK / 库）**必须**按以下结构组织：

```
my-project/
├── cmd/                    # 可执行程序入口（每个子目录一个 main）
│   └── server/
│       └── main.go
├── internal/               # 私有应用代码（外部无法 import）
│   ├── app/
│   ├── handler/
│   ├── service/
│   └── repository/
├── pkg/                    # 公开库代码（外部可 import）
│   └── client/
├── api/                    # API 定义（proto / openapi / graphql）
├── configs/                # 配置文件模板
├── deployments/            # 部署相关（Dockerfile / k8s / terraform）
├── scripts/                # 运维脚本（Makefile / shell）
├── test/                   # 集成测试 / 测试数据 / 测试工具
├── docs/                   # 设计文档 / 架构图
├── go.mod
├── go.sum
├── README.md
└── LICENSE
```

🟡 推荐：

- **业务代码放 `internal/`**——Go 编译器强制阻止外部 import，避免 API 泄露
- **可复用库放 `pkg/`**——内部约定"pkg 是公开 API"
- **每个可执行程序单独目录**——`cmd/server`、`cmd/cli` 互不影响

❌ 不要：

- 把所有代码塞在根目录
- 在 `pkg/` 放业务代码（应该 `internal/`）
- 用 `src/` 目录（Java 风格）

### 1.3 入口文件

```go
// cmd/server/main.go
package main

import (
    "context"
    "log"
    "os"
    "os/signal"
    "syscall"

    "github.com/your-org/your-project/internal/app"
)

func main() {
    ctx, cancel := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
    defer cancel()

    a, err := app.New(ctx)
    if err != nil {
        log.Fatalf("init app: %v", err)
    }
    defer a.Close()

    if err := a.Run(ctx); err != nil {
        log.Fatalf("run app: %v", err)
    }
}
```

🔴 必加约束：

- `main()` 函数**只**做三件事：**初始化 / 运行 / 优雅退出**
- 业务逻辑**必须在** `internal/app/` 下的 `Run(ctx)` 方法中
- 捕获 `SIGINT` / `SIGTERM` 实现优雅退出

---

## 2. 静态检查：golangci-lint

🔴 必加约束：CI 必须运行与仓库当前工具链相容的静态检查，至少包含 `go vet` 与项目已经约定的检查。`.golangci.yml` 和 `golangci-lint run` 仅适用于已选用 golangci-lint 的项目；不要为套用本节模板擅自引入工具。

### 2.1 最小配置

```yaml
# .golangci.yml
run:
  timeout: 5m
  go: '1.21'
  tests: true

linters:
  disable-all: true
  enable:
    - errcheck       # 检查 error 是否被忽略
    - govet          # Go 官方检查
    - ineffassign    # 无效赋值
    - staticcheck    # 高级静态分析（SA* 系列）
    - unused         # 未使用变量 / 函数 / 导入

linters-settings:
  govet:
    enable-all: true
    disable:
      - fieldalignment  # 太严，禁用
```

### 2.2 推荐 Linters

| Linter | 用途 | 优先级 |
|---|---|---|
| `errcheck` | 未检查 error | 🟡 若项目使用 golangci-lint，推荐 |
| `govet` | 官方检查（含 printf / shadow） | 🔴 必加（可直接运行 `go vet`） |
| `staticcheck` | 高级静态分析 | 🟡 按项目工具链选择 |
| `ineffassign` | 无效赋值（如 `x = x`） | 🟡 按项目工具链选择 |
| `unused` | 未使用代码 | 🟡 按项目工具链选择 |
| `gocritic` | 100+ 检查项（性能 / 风格） | 🟡 推荐 |
| `gocyclo` | 圈复杂度（阈值 15） | 🟡 推荐 |
| `gofmt` / `gofumpt` | 比 gofmt 更严格 | 🟡 推荐 |
| `prealloc` | slice 预分配检查 | 🟢 可选 |
| `revive` | golint 替代 | 🟢 可选 |
| `nlreturn` | `return` 前必须有空行 | 🟢 可选 |
| `gocognit` | 认知复杂度 | 🟢 可选 |
| `misspell` | 拼写检查 | 🟢 可选 |

### 2.3 配置继承

🟡 推荐：项目内部可分层继承。

```yaml
# .golangci.yml
linters:
  presets:
    - std  # 标准检查集（基础）
    - bugs # bug 检测集
```

---

## 3. Makefile 任务自动化

🟡 推荐：为多步骤或多人协作项目提供统一命令入口。可使用 `Makefile`、`Taskfile`、脚本或仓库已有任务系统；小型库或已有 CI 约定不应仅为套用本节强制引入 `Makefile`。

### 3.1 最小模板

```makefile
# Makefile
BINARY := $(notdir $(CURDIR))
GO    ?= go

.PHONY: help build run test lint fmt tidy clean

help:           ## Show this help
	@awk 'BEGIN {FS = ":.*##"; printf "Usage:\n  make \033[36m<target>\033[0m\n"} /^[a-zA-Z_-]+:.*?##/ { printf "  \033[36m%-15s\033[0m %s\n", $$1, $$2 }' $(MAKEFILE_LIST)

build:          ## Build binary to bin/
	mkdir -p bin
	$(GO) build -ldflags="-s -w" -o bin/$(BINARY) ./cmd/server

run:            ## Run server locally
	$(GO) run ./cmd/server

test:           ## Run unit tests
	$(GO) test -race -cover ./...

lint:           ## Run golangci-lint
	golangci-lint run ./...

fmt:            ## Format code
	$(GO) fmt ./...
	gofmt -s -w .

tidy:           ## Tidy go.mod
	$(GO) mod tidy

clean:          ## Clean build artifacts
	rm -rf bin/
```

### 3.2 推荐目标

| 目标 | 作用 |
|---|---|
| `make build` | 编译二进制到 `bin/` |
| `make run` | 本地运行 |
| `make test` | 跑单元测试 |
| `make lint` | 跑 golangci-lint |
| `make fmt` | 格式化代码 |
| `make tidy` | 整理 go.mod |
| `make clean` | 清理构建产物 |
| `make docker-build` | 构建 Docker 镜像 |
| `make docker-run` | 跑 Docker 容器 |

🔴 必加约束：

- **`make help`** 必须输出可用目标列表（用 `awk` 自动提取 `##` 注释）
- 所有目标用 `.PHONY` 声明（避免与同名文件冲突）

❌ 不要：

- 用 `bash` 脚本代替 Makefile（可移植性差）
- 在 Makefile 里硬编码绝对路径

---

## 4. CI/CD 配置

🔴 必加约束：项目**必须**配 GitHub Actions（或同等 CI），**每次** push / PR 触发以下流程。

### 4.1 GitHub Actions 最小流程

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: '1.21'
          cache: true
      - name: go mod download
        run: go mod download
      - name: gofmt
        run: |
          unformatted=$(find . -path ./vendor -prune -o -name '*.go' -exec gofmt -l {} +)
          if [ -n "$unformatted" ]; then
            echo "gofmt failed:"
            printf '%s\n' "$unformatted"
            exit 1
          fi
      - name: go vet
        run: go vet ./...
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v3
        with:
          version: latest
      - name: test
        run: go test -race -coverprofile=coverage.out ./...
      - name: build
        run: go build -o bin/server ./cmd/server
```

### 4.2 必跑步骤

| 步骤 | 工具 | 失败处理 |
|---|---|---|
| 依赖下载 | `go mod download` | — |
| 格式检查 | `gofmt -l` | 失败则 PR 不可合 |
| 静态检查 | `go vet` | 失败则 PR 不可合 |
| 静态检查 | `golangci-lint run` | 失败则 PR 不可合 |
| 单元测试 | `go test -race` | 失败则 PR 不可合 |
| 编译 | `go build` | 失败则 PR 不可合 |

🟡 推荐：

- 测试覆盖率 ≥ 70%
- 自动上传 coverage 到 Codecov / Coveralls
- 缓存 `~/.cache/go-build` 和 `~/go/pkg/mod` 加速 CI

---

## 5. 环境配置

### 5.1 配置加载（推荐 viper / envconfig）

🟡 推荐：用 [`spf13/viper`](https://github.com/spf13/viper) 或 [`kelseyhightower/envconfig`](https://github.com/kelseyhightower/envconfig) 加载配置。

```go
// 用 envconfig
type Config struct {
    Server   ServerConfig   `envconfig:"SERVER"`
    Database DatabaseConfig `envconfig:"DATABASE"`
    LogLevel string          `envconfig:"LOG_LEVEL" default:"info"`
}

type ServerConfig struct {
    Port         int           `envconfig:"PORT" default:"8080"`
    ReadTimeout  time.Duration `envconfig:"READ_TIMEOUT" default:"10s"`
    WriteTimeout time.Duration `envconfig:"WRITE_TIMEOUT" default:"10s"`
}
```

🔴 必加约束：

- 配置**必须**支持环境变量
- 配置**必须**有默认值
- 敏感信息（密码 / 密钥）**必须**从环境变量读取，不进代码库

### 5.2 12-Factor App 配置原则

🟡 推荐：

| 原则 | 实施 |
|---|---|
| 配置存环境变量 | 不进代码库 |
| 显式声明依赖 | `go.mod` 完整 |
| 配置存环境 | 不用本地配置文件（生产环境） |
| 后端服务当附加资源 | DB / Redis / MQ 走 URL |

❌ 不要：

- 配置硬编码（如 `const apiKey = "xxx"`）
- 配置文件进 Git（含密码 / 密钥）
- 不同环境用不同分支（用环境变量区分）

---

## 6. 构建配置

### 6.1 编译标志（ldflags）

🔴 必加约束：二进制**必须**注入版本号 / 提交 SHA / 构建时间。

```makefile
# Makefile 中的 build 目标
VERSION    := $(shell git describe --tags --always --dirty)
COMMIT     := $(shell git rev-parse --short HEAD)
BUILD_TIME := $(shell date -u '+%Y-%m-%dT%H:%M:%SZ')
LDFLAGS    := -s -w \
             -X 'main.Version=$(VERSION)' \
             -X 'main.Commit=$(COMMIT)' \
             -X 'main.BuildTime=$(BUILD_TIME)'

build:
	$(GO) build -ldflags="$(LDFLAGS)" -o bin/$(BINARY) ./cmd/server
```

```go
// cmd/server/main.go
package main

var (
    Version   = "dev"
    Commit    = "unknown"
    BuildTime = "unknown"
)
```

### 6.2 交叉编译

🟡 推荐：在 Makefile 提供交叉编译目标。

```makefile
.PHONY: build-all
build-all: ## Build for all platforms
	GOOS=linux   GOARCH=amd64 $(GO) build -o bin/$(BINARY)-linux-amd64   ./cmd/server
	GOOS=linux   GOARCH=arm64 $(GO) build -o bin/$(BINARY)-linux-arm64   ./cmd/server
	GOOS=darwin  GOARCH=amd64 $(GO) build -o bin/$(BINARY)-darwin-amd64  ./cmd/server
	GOOS=darwin  GOARCH=arm64 $(GO) build -o bin/$(BINARY)-darwin-arm64  ./cmd/server
	GOOS=windows GOARCH=amd64 $(GO) build -o bin/$(BINARY)-windows-amd64.exe ./cmd/server
```

### 6.3 Docker 多阶段构建

🟡 推荐：用多阶段构建减小镜像体积。

```dockerfile
# Dockerfile
# ---- 阶段 1：构建 ----
FROM golang:1.21-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /server ./cmd/server

# ---- 阶段 2：运行 ----
FROM alpine:3.18

RUN apk --no-cache add ca-certificates
WORKDIR /app
COPY --from=builder /server /app/server

EXPOSE 8080
ENTRYPOINT ["/app/server"]
```

🔴 必加约束：

- 使用多阶段构建（最终镜像 < 20MB）
- 用 `alpine` 或 `scratch` 而非 `ubuntu`（减小体积）
- 设 `CGO_ENABLED=0`（避免 glibc 依赖）
- 非 root 用户运行

---

## 7. AI 项目配置清单

AI 创建 / 修改 Go 项目配置时，必须按此清单核查：

### 7.1 基础结构

- [ ] `go.mod` 含完整 module 路径（Git URL 形式）
- [ ] Go 版本 ≥ 1.21（当前 LTS）
- [ ] 业务代码在 `internal/`，可复用库在 `pkg/`
- [ ] `cmd/` 下每个可执行程序独立目录
- [ ] `main.go` 只做初始化 / 运行 / 优雅退出

### 7.2 静态检查

- [ ] `.golangci.yml` 存在并启用 errcheck / govet / staticcheck / ineffassign / unused
- [ ] CI 配置运行 `gofmt -l` 检查
- [ ] CI 配置运行 `golangci-lint run`

### 7.3 构建与发布

- [ ] Makefile 含 build / test / lint / fmt / tidy / clean / help
- [ ] `make help` 可用
- [ ] ldflags 注入 Version / Commit / BuildTime
- [ ] Dockerfile 多阶段构建
- [ ] 非 root 用户运行容器

### 7.4 CI/CD

- [ ] GitHub Actions 在 push / PR 时触发
- [ ] 必跑：gofmt / go vet / golangci-lint / go test / go build
- [ ] 测试覆盖率 ≥ 70%
- [ ] CI 失败 PR 不可合并（保护分支）

### 7.5 配置与环境

- [ ] 敏感信息从环境变量读取
- [ ] 配置有默认值
- [ ] 12-Factor App 原则遵守（配置与代码分离）

---

> 💡 完成上述 5 大类 = Go 项目配置最小可用基线。
